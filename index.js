const divide = function(numerator, denominator){
     return numerator / denominator;
};
const result = divide(2000, 100);
console.log(result);

const square = (num) => {
    return num * num;
  };

const add = (num, den) => {
    return num + den;

};